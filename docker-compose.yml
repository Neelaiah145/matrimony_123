services:
  web:
    build: .
    container_name: matrimony2_deployment

    command: gunicorn config.wsgi:application --bind 0.0.0.0:8000

    volumes:
      - .:/app

    ports:
      - "8001:8000"

    env_file:
      - .env

    restart: unless-stopped