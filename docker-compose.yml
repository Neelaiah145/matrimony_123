services:
  web:
    build: .
    container_name: matrimony_backend

    command: python manage.py runserver 0.0.0.0:8000

    volumes:
      - .:/app

    ports:
      - "8000:8000"

    env_file:
      - .env

    environment:
      DB_HOST: host.docker.internal

    restart: unless-stopped